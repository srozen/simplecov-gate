# frozen_string_literal: true

source "https://rubygems.org"

gem "minitest"
# CI's test matrix overrides the version to prove the action against
# every SimpleCov report format it supports.
gem "simplecov", ENV.fetch("SIMPLECOV_VERSION", "~> 1.1")
